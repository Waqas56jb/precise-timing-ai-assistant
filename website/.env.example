# Chat widget backend URL.
# Leave unset to auto-detect: dev (:5173) -> http://127.0.0.1:3001, production -> same origin.
# Set explicitly when the API lives on another domain, e.g.:
# VITE_CHAT_API_URL=https://api.precisetimingtransports.com
VITE_CHAT_API_URL=
